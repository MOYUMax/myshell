sudo ovs-vsctl set bridge s1 protocols=OpenFlow13
sudo ovs-vsctl set bridge s2 protocols=OpenFlow13
sudo ovs-vsctl set bridge s3 protocols=OpenFlow13
sudo ovs-vsctl set bridge s4 protocols=OpenFlow13
#sudo ovs-ofctl set br s1 -O protocols=OpenFlow13
#sudo ovs-vsctl set br s2 -O protocols=OpenFlow13
#sudo ovs-vsctl set br s3 -O protocols=OpenFlow13
#sudo ovs-vsctl set br s4 -O protocols=OpenFlow13
#sudo ovs-vsctl set br s9 protocols=OpenFlow13
#sudo ovs-vsctl set br s10 protocols=OpenFlow13
#sudo ovs-vsctl set br s11 protocols=OpenFlow13
#sudo ovs-vsctl set br s12 protocols=OpenFlow13
#sudo ovs-vsctl set br s13 protocols=OpenFlow13

